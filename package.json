{
  "name": "root",
  "private": true,
  "scripts": {
    "release": "lerna publish",
    "build": "lerna run build --parallel"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^16.10.2",
    "babel-core": "7.0.0-bridge.0",
    "jest": "^27.2.5",
    "lerna": "^4.0.0",
    "typescript": "^4.4.3"
  }
}
